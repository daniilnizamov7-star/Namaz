<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
  <meta name="theme-color" content="#0f766e">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
  <title>Намаз Челябинск</title>
  <link rel="manifest" href="/manifest.json">
  <link rel="icon" type="image/png" href="/icon.png">
  <style>
    :root { --bg: #f8fafc; --card: #ffffff; --primary: #0f766e; --text: #0f172a; --muted: #64748b; }
    * { box-sizing: border-box; margin: 0; padding: 0; -webkit-tap-highlight-color: transparent; }
    body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; background: var(--bg); color: var(--text); line-height: 1.5; min-height: 100vh; display: flex; flex-direction: column; }
    header { background: linear-gradient(135deg, var(--primary), #065f46); color: white; padding: 20px 16px; text-align: center; border-radius: 0 0 24px 24px; }
    h1 { font-size: 1.5rem; font-weight: 600; margin-bottom: 4px; }
    .date { font-size: 0.95rem; opacity: 0.9; }
    .offline-badge { display: inline-flex; align-items: center; gap: 6px; background: rgba(255,255,255,0.2); padding: 4px 10px; border-radius: 12px; font-size: 0.8rem; margin-top: 8px; }
    .main { flex: 1; padding: 16px; max-width: 480px; margin: 0 auto; width: 100%; }
    .countdown-box { background: var(--card); padding: 20px; border-radius: 16px; text-align: center; box-shadow: 0 2px 8px rgba(0,0,0,0.05); margin: 16px 0; border: 1px solid #e2e8f0; }
    .countdown-label { font-size: 0.9rem; color: var(--muted); margin-bottom: 4px; }
    .countdown-timer { font-size: 2.2rem; font-weight: 700; color: var(--primary); font-variant-numeric: tabular-nums; }
    .next-prayer { font-size: 1rem; font-weight: 500; margin-bottom: 8px; }
    .qibla-box { background: var(--card); padding: 16px; border-radius: 16px; text-align: center; box-shadow: 0 2px 8px rgba(0,0,0,0.05); margin-bottom: 16px; border: 1px solid #e2e8f0; }
    .qibla-title { font-weight: 600; margin-bottom: 8px; }
    .qibla-deg { font-size: 1.5rem; color: var(--primary); }
    .qibla-hint { font-size: 0.85rem; color: var(--muted); margin-top: 4px; }
    .compass-container { width: 180px; height: 180px; margin: 12px auto; position: relative; border: 2px solid #e2e8f0; border-radius: 50%; background: #f1f5f9; }
    .compass-needle { position: absolute; top: 50%; left: 50%; width: 4px; height: 70px; background: #ef4444; transform-origin: bottom center; transform: translate(-50%, -100%) rotate(0deg); border-radius: 2px; }
    .compass-n { position: absolute; top: 8px; left: 50%; transform: translateX(-50%); font-weight: bold; color: #ef4444; }
    .compass-kaaba { position: absolute; top: 12px; left: 50%; width: 20px; height: 20px; background: var(--primary); border-radius: 4px; transform: translateX(-50%); display: flex; align-items: center; justify-content: center; color: white; font-size: 10px; }
    .compass-btn { display: inline-block; margin-top: 12px; padding: 10px 16px; background: var(--primary); color: white; border-radius: 10px; text-decoration: none; font-weight: 500; border: none; cursor: pointer; }
    .compass-btn:disabled { opacity: 0.6; cursor: default; }
    .schedule-box { background: var(--card); padding: 16px; border-radius: 16px; box-shadow: 0 2px 8px rgba(0,0,0,0.05); margin-bottom: 16px; border: 1px solid #e2e8f0; }
    .schedule-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 12px; }
    .schedule-title { font-weight: 600; }
    .share-btn { background: none; border: 1px solid var(--primary); color: var(--primary); padding: 6px 12px; border-radius: 8px; font-size: 0.85rem; cursor: pointer; }
    .prayer-row { display: flex; justify-content: space-between; padding: 10px 0; border-bottom: 1px solid #f1f5f9; }
    .prayer-row:last-child { border-bottom: none; }
    .prayer-name { font-weight: 500; }
    .prayer-time { font-family: monospace; font-size: 1.1rem; }
    .prayer-row.active { background: #ecfdf5; border-radius: 8px; padding: 10px 8px; border-bottom: none; }
    .prayer-row.active .prayer-name { color: var(--primary); font-weight: 600; }
    .prayer-row.past { opacity: 0.5; }
    .disclaimer { font-size: 0.8rem; color: var(--muted); text-align: center; padding: 16px; background: #fef3c7; border-radius: 10px; margin-bottom: 20px; }
    .update-note { font-size: 0.75rem; color: var(--muted); text-align: center; margin-top: 8px; }
    footer { text-align: center; padding: 16px; font-size: 0.75rem; color: var(--muted); }
    @media (prefers-color-scheme: dark) {
      :root { --bg: #0f172a; --card: #1e293b; --text: #f8fafc; --muted: #94a3b8; }      .offline-badge { background: rgba(255,255,255,0.1); }
      .disclaimer { background: #451a03; color: #fde68a; }
      .compass-container { background: #334155; border-color: #475569; }
    }
  </style>
</head>
<body>
  <header>
    <h1>Намаз Челябинск</h1>
    <div class="date">Зуль-Кагда 1447</div>
    <div id="offlineStatus" class="offline-badge" style="display:none;">📶 Офлайн-режим. Данные из кэша.</div>
  </header>

  <main class="main">
    <div class="countdown-box">
      <div class="next-prayer" id="nextPrayerName">До следующего намаза</div>
      <div class="countdown-timer" id="countdown">--:--:--</div>
    </div>

    <div class="qibla-box">
      <div class="qibla-title">🕋 Направление Киблы</div>
      <div class="qibla-deg">Для Челябинска (229°) СЮЗВ</div>
      <div class="compass-container">
        <span class="compass-n">С</span>
        <div class="compass-kaaba">🕋</div>
        <div class="compass-needle" id="needle"></div>
      </div>
      <div class="qibla-hint">Нажмите кнопку ниже</div>
      <button class="compass-btn" id="compassBtn">📡 Включить компас</button>
      <div class="qibla-hint" style="margin-top:8px;">Держите телефон горизонтально. Красная С = Север. Зелёная стрелка = Кааба.</div>
      <a href="https://qiblafinder.withgoogle.com" target="_blank" class="compass-btn" style="background:#3b82f6; margin-top:8px;">🌍 Открыть точный Qibla Finder в браузере</a>
    </div>

    <div class="schedule-box">
      <div class="schedule-header">
        <span class="schedule-title">Расписание на месяц</span>
        <button class="share-btn" onclick="shareSchedule()">📤 Поделиться</button>
      </div>
      <div id="prayerList">Загрузка...</div>
    </div>

    <div class="disclaimer">
      ⚠️ Время намазов указано справочно. Для обязательств сверяйтесь с расписанием местной мечети.
      <div class="update-note">Обновляется автоматически в 00:00</div>
    </div>
  </main>

  <footer>Данные: ЦДУМ России / РДУМ Челябинской обл.</footer>

  <script>    // === ТВОЁ РАСПИСАНИЕ ===
    const PRAYERS = [
      { key: 'fajr', name: 'Фаджр', time: '04:32' },
      { key: 'sunrise', name: 'Шурук', time: '06:15' },
      { key: 'dhuhr', name: 'Зухр', time: '12:18' },
      { key: 'asr', name: 'Аср', time: '16:45' },
      { key: 'maghrib', name: 'Магриб', time: '18:20' },
      { key: 'isha', name: 'Иша', time: '19:45' }
    ];

    let nextPrayerObj = null;

    function findNextPrayer() {
      const now = new Date();
      for (const p of PRAYERS) {
        const [h, m] = p.time.split(':');
        const d = new Date(); d.setHours(h, m, 0, 0);
        if (d > now) return { ...p, date: d };
      }
      const [h, m] = PRAYERS[0].time.split(':');
      const d = new Date(); d.setDate(d.getDate() + 1); d.setHours(h, m, 0, 0);
      return { ...PRAYERS[0], date: d };
    }

    function renderList() {
      const now = new Date();
      const container = document.getElementById('prayerList');
      let html = '';
      nextPrayerObj = findNextPrayer();

      PRAYERS.forEach(p => {
        const [h, m] = p.time.split(':');
        const d = new Date(); d.setHours(h, m, 0, 0);
        const isActive = p.key === nextPrayerObj.key;
        const isPast = d < now && !isActive;
        html += `<div class="prayer-row ${isActive ? 'active' : ''} ${isPast ? 'past' : ''}">
          <span class="prayer-name">${p.name}</span>
          <span class="prayer-time">${p.time}</span>
        </div>`;
      });
      container.innerHTML = html;
      document.getElementById('nextPrayerName').textContent = `До ${nextPrayerObj.name}`;
    }

    function startCountdown() {
      const el = document.getElementById('countdown');
      setInterval(() => {
        if (!nextPrayerObj) return;
        const diff = nextPrayerObj.date - new Date();
        const hh = String(Math.floor(diff / 3600000)).padStart(2, '0');        const mm = String(Math.floor((diff % 3600000) / 60000)).padStart(2, '0');
        const ss = String(Math.floor((diff % 60000) / 1000)).padStart(2, '0');
        el.textContent = `${hh}:${mm}:${ss}`;
      }, 1000);
    }

    // === КОМПАС ===
    const compassBtn = document.getElementById('compassBtn');
    const needle = document.getElementById('needle');
    const QIBLA_DEG = 229;

    compassBtn.addEventListener('click', async () => {
      if (typeof DeviceOrientationEvent !== 'undefined' && typeof DeviceOrientationEvent.requestPermission === 'function') {
        const perm = await DeviceOrientationEvent.requestPermission();
        if (perm !== 'granted') return alert('Разрешение на компас отклонено');
      }
      window.addEventListener('deviceorientation', (e) => {
        let heading = e.alpha;
        if (e.webkitCompassHeading) heading = e.webkitCompassHeading;
        if (heading === null) return;
        const rotation = heading + QIBLA_DEG;
        needle.style.transform = `translate(-50%, -100%) rotate(${rotation}deg)`;
        compassBtn.textContent = '✅ Компас активен';
        compassBtn.disabled = true;
      }, { once: true });
    });

    // === ОФЛАЙН / ШЕРИНГ / SW / АВТООБНОВЛЕНИЕ ===
    function checkOnline() {
      const b = document.getElementById('offlineStatus');
      const u = () => b.style.display = navigator.onLine ? 'none' : 'inline-flex';
      window.addEventListener('online', u); window.addEventListener('offline', u); u();
    }

    function registerSW() { if ('serviceWorker' in navigator) navigator.serviceWorker.register('/sw.js').catch(console.error); }

    function setupAutoUpdate() { setInterval(() => { const n = new Date(); if (n.getHours()===0 && n.getMinutes()===0 && n.getSeconds()<5) location.reload(); }, 10000); }

    async function shareSchedule() {
      const txt = `🕌 Намазы (Челябинск)\n${PRAYERS.map(p=>`${p.name}: ${p.time}`).join('\n')}\n📲 PWA Намаз Челябинск`;
      if (navigator.share) await navigator.share({ title: 'Намаз Челябинск', text: txt });
      else { navigator.clipboard.writeText(txt); alert('📋 Скопировано'); }
    }

    // === ДОБАВЛЕНО: УВЕДОМЛЕНИЯ (НЕ ТРОГАЮТ ИНТЕРФЕЙС) ===
    (async function initPrayerNotifications() {
      if (!('Notification' in window) || !('serviceWorker' in navigator)) return;
      if (Notification.permission !== 'granted') {
        const perm = await Notification.requestPermission();
        if (perm !== 'granted') return;      }
      const reg = await navigator.serviceWorker.ready;
      if (!nextPrayerObj) return;

      const triggerMs = nextPrayerObj.date.getTime() - 5 * 60 * 1000;
      const delay = triggerMs - Date.now();

      if (delay > 0 && delay < 24 * 60 * 60 * 1000) {
        localStorage.setItem('pending_prayer_notif', JSON.stringify({ 
          name: nextPrayerObj.name, time: nextPrayerObj.time, at: triggerMs, tag: nextPrayerObj.key 
        }));
        const timer = setTimeout(async () => {
          await reg.showNotification(`⏰ ${nextPrayerObj.name} через 5 мин`, {
            body: `Челябинск • ${nextPrayerObj.time}`, icon: '/icon.png', tag: nextPrayerObj.key, requireInteraction: false
          });
          localStorage.removeItem('pending_prayer_notif');
        }, delay);
        window.addEventListener('pagehide', () => clearTimeout(timer), { once: true });
      }

      // Восстановление после перезагрузки
      const raw = localStorage.getItem('pending_prayer_notif');
      if (raw) {
        const data = JSON.parse(raw);
        const d = data.at - Date.now();
        if (d > 0) {
          setTimeout(async () => {
            await reg.showNotification(`⏰ ${data.name} через 5 мин`, {
              body: `Челябинск • ${data.time}`, tag: data.tag, requireInteraction: false
            });
            localStorage.removeItem('pending_prayer_notif');
          }, d);
        } else {
          localStorage.removeItem('pending_prayer_notif');
        }
      }
    })();

    // ЗАПУСК
    document.addEventListener('DOMContentLoaded', () => {
      renderList(); 
      startCountdown(); 
      checkOnline(); 
      registerSW(); 
      setupAutoUpdate();
    });
  </script>
</body>
</html>